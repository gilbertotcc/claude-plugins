---
type: llm
focus: {source: file, path: docs/style-guide.md}
weight: 0.5
---
- The bulleted list uses `-` markers, not `*` or `+`.
- The prose paragraph is not artificially hard-wrapped at a short line
  length (e.g. ~80 characters); a long sentence runs on one line.
- The document defers to the repository's own `.markdownlint-cli2.yaml`
  config rather than applying a different personal style.
