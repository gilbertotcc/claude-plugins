---
max_turns: 8
timeout_seconds: 120
allowed_tools: [Skill]
runs: 3
---
I just finished a PR that fixes a pagination bug in the search API (it was
double-counting the last page). Draft a PR description in Markdown —
summary, what changed, and a test plan — that I can paste into the GitHub
PR form.
