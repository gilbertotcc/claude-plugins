---
type: llm
focus: {source: file, path: docs/api.md}
weight: 1
---
- The new `/export` section uses the same heading level and formatting
  style as the existing `/users` and `/orders` sections.
- The example response showing the header row `id,name,total` is present,
  formatted clearly (e.g. in a code block).
- No personal style choices were introduced that diverge from the rest of
  the document (e.g. different heading punctuation, different list marker).
