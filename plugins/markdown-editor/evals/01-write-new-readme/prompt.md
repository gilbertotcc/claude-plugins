---
max_turns: 12
timeout_seconds: 240
allowed_tools: [Write, Edit]
runs: 3
---
Write a README.md for this repository. It's a small CLI tool called `tform`
that converts CSV files into formatted ASCII tables.

Include:

- A one-paragraph description of what it does
- Installation instructions (`npm install -g tform`)
- At least two usage examples, each with a sample command and its output in
  a code block
