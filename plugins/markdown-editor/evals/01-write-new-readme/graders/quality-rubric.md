---
type: llm
focus: {source: file, path: README.md}
weight: 1
---
- The README includes a one-paragraph description of what `tform` does (a
  CSV-to-ASCII-table CLI tool).
- It includes installation instructions.
- It includes at least two usage examples, each showing a command and its
  sample output in a code block.
- The formatting matches markdownlint-cli2's default conventions (ATX
  headings, consistent list markers, blank lines around block elements)
  rather than an unusual personal style.
- The document has exactly one top-level (`#`) heading.
- Every fenced code block's opening fence includes a language tag (e.g.
  ```` ```bash ```` or ```` ```text ````), not a bare ```` ``` ````.
- The content is specific to a CSV-to-table CLI tool, not generic filler.
