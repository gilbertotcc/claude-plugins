{
  "description": "Runs markdownlint-cli2 --fix on Markdown files after each Edit/Write, auto-fixing what it can and surfacing anything it can't back to Claude.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/lint-edited-file.sh",
            "timeout": 60,
            "statusMessage": "Linting Markdown..."
          }
        ]
      }
    ]
  }
}
